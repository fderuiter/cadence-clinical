import { apiClient } from "../api/apiClient";

/**
 * Centered HTTP Client Service Wrapper
 * Under apps/web/src/services/api.js
 * Accesses and interacts with the API Gateway dynamically via VITE_API_BASE_URL.
 */
export default apiClient;
