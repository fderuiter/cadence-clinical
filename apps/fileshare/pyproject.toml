[project]
name = "cadence-fileshare"
version = "0.1.0"
description = "Cadence File Sharing, Secure Transfers & Media Management Microservice"
requires-python = ">=3.14"
dependencies = [
    "fastapi",
    "sqlalchemy[asyncio]",
    "aiosqlite",
    "asyncpg",
    "pydantic>=2",
    "httpx",
]
