# Software Requirements Specification (SRS)

## 1. Scope & Introduction
This document defines the compliant audit and ledger architecture of the Cadence Clinical platform, ensuring traceability and regulatory verification for GxP processes. The system synthesizes upstream Clinical Metadata Management via the Designer Service (Neo4j) with downstream Electronic Data Capture via the Execution Engine (PostgreSQL). By leveraging immutable ledgers, graph-based historical tracking, and comprehensive cryptographic mechanisms, Cadence Clinical guarantees data integrity and simplifies compliance verification.

## 2. Dedicated Compliance Section (21 CFR Part 11 / Annex 11)
Cadence Clinical maps directly to the regulatory mandates established by the FDA (21 CFR Part 11) and EMA (Annex 11) to secure electronic records and signatures.

* **21 CFR § 11.10(a) Validation:** The system maintains a continuous record of configuration paths to ensure reproducible behavior across study states, thereby verifying that the system performs as expected under all historical contexts.
* **21 CFR § 11.10(e) Audit Trails:** All clinical modifications generate automated, secure, and chronological time-stamped audit trails. The system natively prevents the alteration or deletion of audit data.
* **21 CFR § 11.10(g) Authority Checks:** Access to system configurations and clinical modifications is governed by role-based access control tied to user identities and execution privileges, logged on every mutation.
* **21 CFR § 11.10(h) Device Checks:** Terminal and session identities are captured alongside each logical transaction to guarantee the source of data entry.

**Standard Audit Fields:**
Every transactional record throughout the platform includes the following mandatory fields:
* `created_at`: The precise UTC timestamp of the transaction.
* `created_by`: The unique identifier/OIDC subject of the executing user.
* `reason_for_change`: A mandatory, user-supplied or system-generated justification for the modification.
* `version_index`: An incremental integer sequence securing the temporal order of events for a specific clinical entity.

## 3. Application-Layer Event Tracking
Within the EDC Execution Engine, application-layer listeners intercept every state change regarding clinical subject metadata (e.g., subject ID, status, enrollment date, demographic records). Before committing any changes to the PostgreSQL operational tables, these listeners synchronously generate an event object encapsulating the pre-mutation state, the post-mutation state, and the contextual audit fields. This ensures that clinical subject histories capture both the *how* and *why* behind data evolution without degrading transactional throughput.

## 4. Cryptographic Ledger Workflow
To provide non-repudiation and prevent data tampering, the platform utilizes a cryptographic ledger mechanism.
* **Background Execution Cycle:** A background scheduler continuously sweeps new, unsealed audit records generated by the application layer.
* **Cryptographic Hashing & Sealing:** Each block of audit logs is cryptographically hashed using robust algorithms (e.g., SHA-256). Every new block hash includes the hash of the immediately preceding block, creating an unbreakable chain.
* **Verification Rules:** Background verification routines independently recalculate these chains. Any anomaly—such as a missing entry or a compromised hash—instantly raises high-priority alerts to compliance officers, identifying the exact timestamp and entry of the tampering event.

## 5. Database-Level Auditing Triggers
As a fail-safe mechanism against direct database access or administrative scripts bypassing the application layer, the PostgreSQL Execution Engine leverages immutable database-level triggers.
* **Trigger Architecture:** `AFTER UPDATE` and `AFTER DELETE` triggers are placed on core clinical tables.
* **Shadow Schema Preservation:** If clinical information is hard-deleted or updated outside the standard application routes, these triggers seamlessly capture the delta and insert it into an isolated, secure shadow schema. This ensures that all clinical subject deletions are preserved in the audit log regardless of the vector of operation.

## 6. Graph-Based Study Versioning
The Designer Service utilizes a Neo4j graph database to track the complex evolution of study configurations, including arms, epochs, visits, branches, and elements.
* **Immutable Timelines:** Instead of overriding existing configuration nodes, modifications result in new versioned nodes.
* **Historical State Capture:** Directed relationships represent temporal continuity (e.g., `PREVIOUS_VERSION`). Consequently, the historical timeline of a study state is captured as an immutable graph path, enabling users to reconstruct the precise configuration that was active at any point in the clinical trial lifecycle.

## 7. Privacy Controls & Exporting
Regulatory compliance also dictates the strict protection of Personally Identifiable Information (PII) during audit trail exports for external reviews.
* **User Masking:** During the generation of CSV export files for auditors, the system dynamically applies data masking to sensitive demographic or user identifiers.
* **Cryptographic Identifier Hashing:** Rather than exposing plain-text user/subject IDs, the export engine outputs deterministic cryptographic hashes of the identifiers. This structural accountability allows auditors to track modifications made by the same individual or applied to the same subject across the audit ledger without exposing actual identities.

## 8. Traceability Mappings for Automated Compliance
To simplify compliance verification and satisfy 21 CFR Part 11 auditing requirements, this platform explicitly links abstract regulatory rules to verifiable codebase implementation mechanics.

* **Trace 1: Shadow Schema Retention:** Database-level hard deletes are programmatically blocked by the application layer. Deletion attempts against `AuditLog` or `AuditedModel` raise uncatchable exceptions via the SQLAlchemy listener module located in `apps/execution/database/audit.py`, ensuring a permanent shadow ledger of all system transactions.
* **Trace 2: Cryptographic Key Multi-Sharing & Rotation:** The system utilizes mathematical polynomial splitting (Shamir's Secret Sharing pattern) to split treatment allocation blinding keys, alongside an automatic 365-day rotation scheme for encryption keys. These operations are explicitly enforced by `AllocationKeyManager` in `apps/execution/cryptography.py`.
* **Trace 3: Read-Only Trial Locks & Alert Routing:** Upon detecting any data compromise, the system immediately freezes clinical transactions by throwing `PermissionError` for write operations (in `audit.py`) while permitting authorized `SELECT` queries. Concurrently, high-priority notifications are dispatched to designated contacts (Email, SMS, Webhook) via the `TrialLockManager` module in `apps/execution/trial_lock.py` within one minute.
* **Trace 4: Data-Driven Expected Document Lists (EDLs):** The system implements a data-driven Expected Document List reference data model and site-aware completeness tracking APIs under `/api/v1/etmf/edl` and `/api/v1/etmf/completeness` using the `ExpectedDocument` model to replace hardcoded validation logic with a dynamic backbone.
* **Trace 5: TMF Taxonomy Validation & Integration Assurance:** The eTMF microservice enforces strict catalog-driven classification during document ingestion, rejecting unknown/invalid artifacts or mismatched configurations with HTTP 422, while persisting the resolved taxonomy version and artifact code to ensure compliance.
* **Trace 6: CTMS Monitoring and Site Operations Tracking:** The CTMS microservice implements a structured site monitoring visit lifecycle, milestone tracking, and CRA workload allocation maps. All mutations are secured via OIDC auth, audited via append-only `CTMSAuditLog` entries with explicit change reasons under 21 CFR Part 11 requirements, and covered by automated tests to ensure compliance.

## 9. Quality & CAPA Management Workflow
The Quality & CAPA subsystem (`apps/quality/`) manages clinical protocol deviations, Root Cause Analysis (RCA), and Corrective and Preventive Actions (CAPA) with robust state controls and immutable audit trails to adhere to GxP and FDA 21 CFR Part 11 guidelines.

* **Deviation Recording and Classification:** When protocol deviations (e.g., IP management excursions, eligibility violations, or informed consent errors) occur at a clinical site, authorized users (such as clinical research coordinators, investigators, or CRAs) report them. The system assigns unique identifiers, captures standard fields (such as study/site IDs, titles, detailed descriptions, severity ratings, and deviation types), and tracks them under the initial `REPORTED` status.
* **Root Cause Analysis (RCA) Attachment:** A reported deviation triggers an investigation. General write roles attach an RCA, detailing the methodology (e.g., 5 Whys, Fishbone), investigation details, and a root cause summary. Attaching an RCA automatically transitions the parent deviation status to `RCA_COMPLETE` in an atomic database transaction.
* **CAPA Record Creation:** From an investigation, users create a CAPA record (classified as either `CORRECTIVE` or `PREVENTIVE`). It outlines the concrete action plans, preventive measures, and expected target completion dates. The parent deviation status automatically progresses to `CAPA_INITIATED` to preserve state consistency.
* **Lifecycle State Transitions:** CAPA records follow a strict, one-way workflow state machine:
  $$\text{INITIATED} \longrightarrow \text{UNDER\_REVIEW} \longrightarrow \text{IMPLEMENTATION} \longrightarrow \text{EFFECTIVENESS\_CHECK} \longrightarrow \text{CLOSED} \mid \text{CANCELLED}$$
  Direct jumps or illegal state bypasses are strictly prohibited (e.g., transitioning directly from `UNDER_REVIEW` to `EFFECTIVENESS_CHECK` is blocked by validation logic). Transitions out of terminal states (`CLOSED` or `CANCELLED`) are completely prohibited.
* **Administrative Closure and Deviation Settlement:** Closing a CAPA record automatically settles and transitions the parent deviation to `CLOSED`, ensuring system-wide state coherence.
* **Immutable Append-Only Audit Trail:** Any modification to quality records (creation, detail update, or status transition) forces optimistic locking version checks (`version_index`), requires a non-empty `X-Change-Reason` justification, and atomically writes an immutable, chronological record into `QualityAuditLog`. Direct writes, modifications, or deletions of audit log entries are prohibited at the database schema level.
* **Role-Based Access Controls (RBAC):**
  * **Read-Only Access:** Roles such as `auditor`, `inspector`, `regulatory_inspector`, `viewer`, and `read_only` are restricted to read-only queries (`GET /api/v1/quality/deviations`, `/api/v1/quality/audit-logs`, etc.) and receive HTTP 403 Forbidden for all write mutations.
  * **General Write Access:** Roles such as `cra`, `investigator`, `crc`, `sponsor_dm`, and `admin` can log deviations, update RCAs, and initiate CAPAs.
  * **Quality Oversight Access:** Only designated quality oversight roles (`quality_manager`, `qa_lead`, `quality_oversight`, and `admin`) are authorized to perform terminal transitions (`CLOSED` or `CANCELLED`) on CAPA records. Attempts by other roles are blocked with HTTP 403 Forbidden.

* **Trace 7: Quality & CAPA Traceability and Validation Assurance:** Quality mutations require a secure `X-Change-Reason` header and valid gateway-signed token. Transitions are validated by the FastAPI router in `apps/quality/main.py`. Role permissions are gated via `authorize_quality_write` and `authorize_quality_oversight`. System consistency and audit trial immutability are verified in `tests/test_quality_workflow.py`.

* **Trace 8: eCOA Subject Identity & Gateway Boundary:** The API Gateway (`apps/gateway/main.py`) acts as a secure reverse-proxy filtering gateway. It validates incoming Keycloak JWT credentials and restricts users holding the `Subject` role exclusively to designated ePRO submission endpoints (`/api/v1/interop/epro/submit` and `/api/v1/interop/epro/sync`). Downstream, `verify_subject_identity` in the interop backend binds the user's OIDC sub-claim directly to the payload's `subject_id` to prevent cross-patient database queries or mutations, as verified in `tests/test_interop.py`.

* **Trace 9: ePRO Offline Sync & Part 11 Reconciliation:** The Patient/Subject Portal (`apps/subject-portal`) implements a "Network First, falling back to Cache" offline-resilient PWA service worker. Submissions created offline are queued inside IndexedDB (`SubjectPortalSyncDB`) with a persistent `client_id` and sequential integer `sequence_number` to prevent message loss. Upon reconnection, these are flushed to `/api/v1/interop/epro/sync`. The interop backend reconciles conflict states using deterministic strategies (`CLIENT_WINS`, `SERVER_WINS`, `MERGE`), persisting defeated or superseded records in `EPROSubmissionDefeated` with Part 11 transaction audits, verified in `tests/test_interop_defeated.py`.

* **Trace 10: Simulated Multi-Channel Notification Dispatcher:** The interop scheduler computes subject-specific compliance reminders on demand via `POST /api/v1/interop/reminders/compute` by checking outstanding `SubjectAssignment` due windows against registered `EPROSubmission` timestamps. It creates `SubjectNotification` entries and dispatches asynchronous background simulation tasks across four parallel channels (`EMAIL`, `SMS`, `WEBHOOK`, `IN_APP`), logged transactionally in `InteropAuditLog`, as verified in `tests/test_interop.py`.

* **Trace 11: Native Vue 3 Rules Designer and GxP Ledger Sync:** Clinical study rule configuration and logical expression tree authoring is fully integrated within the Vue 3 client-side router (`apps/web/src/router.js`). Navigation access is secured via a Keycloak role guard restricting access to `STUDY_DESIGNER`. All rule saving and soft deletions capture a mandatory GxP reason-for-change justification, which is cryptographically signed and synchronously appended to the clinical audit ledger, as verified in `tests/test_designer_rules.py` and `apps/web/tests/smoke.test.js`.

* **Trace 12: eTMF Document Redaction & Regulatory Privacy Controls:** The eTMF microservice (`apps/etmf`) provides automated and manual de-identification pipelines under `/api/v1/etmf/documents/{document_id}/auto-redact` and `/manual-redact`. Guided by active regulatory compliance profiles (HIPAA, GDPR, EU CTR), the engine processes document text with regex-based scanners and literal terms, applying sanitizing transforms (masking, pseudonymization, age capping, and a default 365-day date-shifting window). To satisfy GxP trace demands and maintain data integrity, a cryptographic `RedactionManifest` is built, signed symmetrically using HMAC-SHA256, and recorded transactionally in the immutable `TMFAuditLog` (retaining non-sensitive redacted derivative indices while strictly omitting raw matched identifiers from all logs). Unauthorized role personas (e.g., auditors or inspectors) are locked into the redacted view and blocked from accessing raw originals, verified in `tests/test_etmf_redaction.py` and `tests/test_deidentification.py`.

## 10. eCOA Subject Portal & Interoperability Ecosystem
The patient-facing ecosystem consists of a Progressive Web App (PWA) client and an interoperability microservice designed to handle patient diary logs and medical data integrations with absolute privacy and performance safety.

* **Progressive Web App Architecture:** The `apps/subject-portal` front-end is hosted independently of sponsor tools to eliminate accidental administrative UI exposure. It is configured with a custom web app manifest (`manifest.json`) and service worker (`sw.js`) to cache static resources and enable offline access.
* **IndexedDB Sync Queue Panel:** When a user is offline, submissions are signed and committed locally. The portal provides a Sync Queue Panel that displays pending, completed, or failed synchronization states visually, ensuring the patient is fully aware of their data transmission status.
* **Clinical Queries on Sync Exceptions:** If a synchronized record references an Instrument or SubjectAssignment that has been deleted or is missing, the interop engine rejects the entry as a structural conflict. The defeated input is archived for review, and an `OPEN` status `ClinicalQuery` is generated automatically with a system exception change reason to prevent silent data loss.
