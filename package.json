{
  "name": "cadence-clinical-workspace",
  "version": "1.0.0",
  "description": "> **The Metadata-Driven Clinical Execution Platform.** > *Unifying Clinical Study Design (MDR/SDR) and Electronic Data Capture (EDC) into a single, automated digital data flow.*",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "pnpm -r test && uv run pytest",
    "lint": "pnpm -r lint && uv run ruff check .",
    "format": "pnpm -r format && uv run ruff format .",
    "check": "concurrently --kill-others-on-fail false -n \"format,lint,test,links,adr,markdown,security\" -c \"magenta,blue,green,cyan,yellow,gray,red\" \"pnpm format\" \"pnpm lint\" \"pnpm test\" \"node scripts/check-links.js\" \"python3 scripts/validate_adrs.py\" \"python3 scripts/validate_markdown.py\" \"uv run bandit -c pyproject.toml -ll -ii -r apps packages\"",
    "build": "pnpm --filter web build",
    "build:docs": "node scripts/build-docs.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fderuiter/cadence-clinical.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/fderuiter/cadence-clinical/issues"
  },
  "homepage": "https://github.com/fderuiter/cadence-clinical#readme",
  "private": true,
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^10.0.4",
    "eslint": "^10.7.0",
    "eslint-plugin-vue": "^10.10.0",
    "prettier": "^3.9.6",
    "vitepress": "^1.6.4",
    "vitest": "^4.1.10"
  }
}
